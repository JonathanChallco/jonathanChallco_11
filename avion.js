let velocidad = 265;
let altura = 140;

if (velocidad >= 268 && velocidad <= 278 && altura >= 150 && altura <= 300) {
    console.log("El avión está listo para aterrizar");
} else {
    console.log("El avión no está listo para aterrizar.");
}

// se puede hacer con function, declarando un nombre y agregando como (velocidad,altura)
// al final llamando solo a la funcion
